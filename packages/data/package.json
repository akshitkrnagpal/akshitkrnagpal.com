{
  "name": "@akshitkrnagpal/data",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.mjs",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsup src/index.ts --format cjs --dts-resolve --watch",
    "build": "tsup src/index.ts --format cjs --dts-resolve"
  },
  "devDependencies": {
    "tsup": "^7.2.0"
  }
}
